import { BackgorundSwitcher } from "@/components/backgorund-switcher.tsx";

export const Nav = () => {
  return (
    <nav className="w-full flex p-3 flex-row-reverse max-w-7xl">
      <BackgorundSwitcher />
    </nav>
  );
};
